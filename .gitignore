# === zaleznosci / build (Astro) ===
node_modules/
dist/
.astro/

# === Materiały źródłowe wykładowców — NIE publikować (patrz stopka strony) ===
*.pdf
jakość claude/

# === Odpady po "Zapisz stronę jako" w przeglądarce (nowe; już-śledzone zostają) ===
*_files/

# === env ===
.env
.env.*
!.env.example

# === System / edytor ===
.DS_Store
Thumbs.db

# dev-only pages / scratch (nie deployowane)
dev-pages/

# local Claude Code config
.claude/
